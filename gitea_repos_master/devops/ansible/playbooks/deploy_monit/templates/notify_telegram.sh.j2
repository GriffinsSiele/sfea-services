#!/bin/bash

API_TOKEN="{{ api_token }}"
CHAT_ID="{{ chat_id }}"
MESSAGE="$1"

curl -s --max-time 10 -d "chat_id=$CHAT_ID&text=$MESSAGE" "https://api.telegram.org/bot$API_TOKEN/sendMessage" >/dev/null