apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.name" . }}-secrets-http-serve
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.http_serve.env }}
  {{ $k }}: {{ $v | b64enc | quote }}
{{- end }}
