apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart.name" . }}-configmap-metrics
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.metrics.env }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
