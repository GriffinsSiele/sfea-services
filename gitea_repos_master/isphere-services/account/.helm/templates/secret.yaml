apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.name" . }}-secrets
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.app.env }}
  {{ $k }}: {{ $v | b64enc | quote }}
{{- end }}
