apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart.name" . }}-configmap-messenger-consume-async
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.messenger_consume_async.env }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
