apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart.name" . }}-configmap-php-fpm
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.php_fpm.env }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
