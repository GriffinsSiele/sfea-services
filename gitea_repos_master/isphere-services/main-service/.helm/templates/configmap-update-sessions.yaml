apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart.name" . }}-configmap-update-sessions
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.update_sessions.env }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
