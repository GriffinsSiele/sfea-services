apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.name" . }}-secrets-messenger-consume-async
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.messenger_consume_async.env }}
  {{ $k }}: {{ $v | b64enc | quote }}
{{- end }}
