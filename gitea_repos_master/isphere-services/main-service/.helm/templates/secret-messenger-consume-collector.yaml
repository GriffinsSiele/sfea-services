apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.name" . }}-secrets-messenger-consume-collector
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.messenger_consume_collector.env }}
  {{ $k }}: {{ $v | b64enc | quote }}
{{- end }}
