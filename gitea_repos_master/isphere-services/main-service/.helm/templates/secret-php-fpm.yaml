apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.name" . }}-secrets-php-fpm
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.php_fpm.env }}
  {{ $k }}: {{ $v | b64enc | quote }}
{{- end }}
