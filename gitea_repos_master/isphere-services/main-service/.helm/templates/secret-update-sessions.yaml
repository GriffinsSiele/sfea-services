apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.name" . }}-secrets-update-sessions
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.update_sessions.env }}
  {{ $k }}: {{ $v | b64enc | quote }}
{{- end }}
