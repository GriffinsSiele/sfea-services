apiVersion: v1
kind: Service
metadata:
  name: {{ include "isto.fullname" . }}
  labels:
    {{- include "isto.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isto.selectorLabels" . | nindent 4 }}
